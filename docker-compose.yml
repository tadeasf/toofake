services:
  toofake:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: toofake
    ports:
      - "3279:3279"
    environment:
      - NODE_ENV=production
      - PORT=3279
    restart: unless-stopped
